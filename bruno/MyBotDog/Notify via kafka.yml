info:
  name: Notify via kafka
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/{{user}}/notify?question=tiene algún perro en adopción, de mucho pelo? no importa si tiene pulgas, acá lo bañamos"
  params:
    - name: question
      value: tiene algún perro en adopción, de mucho pelo? no importa si tiene pulgas, acá lo bañamos
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
