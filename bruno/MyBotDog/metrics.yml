info:
  name: metrics
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/actuator/metrics/gen_ai.client.operation"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
